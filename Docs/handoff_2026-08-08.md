# Handoff — 2026-08-08

**다음 세션은 어느 PC에서 열려도 이어갈 수 있게 정리함 — 이 문서를 가장 먼저 읽을 것.**

## 가장 먼저 확인할 것

- **`git pull` 먼저.** 이 세션 끝에 아래 "Git" 절 커밋 전부 `origin/main`에 push 완료
  (force-push 아님, fast-forward될 것).
- **Cntl/CAM 둘 다 재빌드+재플래시 필요.** 오늘 소스가 많이 바뀜(설정 소유권 아키텍처,
  Apply UX, ESP-NOW 메시지 추가). 재플래시하면 페어링도 초기화되니 재페어링 필요.
- **COM 포트는 항상 이 PC에서 새로 확인.** 마지막 확인값은 Cntl=COM18/CAM=COM5였지만
  PC/USB포트가 다르면 재할당됨.
- **`Ongoing/` 최상위 폴더 구조가 바뀜.** `Cntl_old`/`Cntl_backup_20260730_224030`/
  `waveshare_43b_vendor_src`는 전부 `_Archive/` 밑으로 이동했고, `cntl_incremental_test`/
  `waveshare_43b_vendor_test`는 완전히 삭제됨(둘 다 실제 소스 없이 gitignore 대상 빌드
  찌꺼기만 남아있던 상태였음). 로컬에 옛 이름 폴더가 남아있어도 그냥 잔재이니 새로 할 일
  없음.
- **`.gitignore`가 `**/.claude/*`로 바뀜** — 최상위뿐 아니라 `Cntl/.claude/`,
  `Sens/.claude/` 등 하위 프로젝트의 로컬 메모리/설정도 이제 동일하게 커밋 제외 대상(단
  `settings.json`은 예외로 계속 git에 포함). 이 PC에 이미 있던 하위 `.claude/` 잔재가
  `git status`에서 사라져도 정상.
- **자동 메모리(`~/.claude/projects/.../memory/`)를 이 세션에서 많이 갱신함** — 특히
  아래 "내일 할 일" 두 항목은 전부 `project_cntl_rtc_and_unified_sleep_plan.md`에 상세히
  적혀 있으니, 다른 PC에서 이어갈 때 그 메모리 파일이 없다면(=이 계정/이 PC의 자동메모리
  디렉터리가 다르다면) 이 문서의 "내일 할 일" 절을 대신 참고할 것.
- **Claude 메모리 이식**: 같은 폴더의 `claude_memory_2026-08-08.zip`을 다른 PC의 해당
  프로젝트 메모리 폴더(`~/.claude/projects/<이 프로젝트의 절대경로 기반 자동생성
  폴더명>/memory/`, 정확한 폴더명은 그 PC에서 실제 생성된 걸 확인)에 풀 것.

## 오늘 한 일 요약

자세한 서술은 `한일_2026-08-08.txt` 참고. 핵심만:

1. PC 마이그레이션 복귀 재확인(COM포트, CAM센서, 드롭다운 한글 깨짐 폰트버그 수정).
2. **아키텍처 전환**: CAM/SENS는 이제 로컬 설정을 저장하지 않고, Cntl에 접속할 때마다
   Cntl이 주는 값(촬영주기: 카메라별, 응답성: 시스템 전역)을 그대로 따름. Cntl에
   `device_config.c/h` 신설, `CAM_CONFIG_SET`/`ACK` ESP-NOW 메시지 추가, 새로 페어링되면
   자동으로 현재 설정을 CAM에 푸시.
3. Option탭에 [라벨][드롭다운][Apply] 인라인 UX 완성 (5단계 시나리오: 비활성→표시→
   변경시활성화→클릭시진행팝업(진행/성공/실패)→재비활성).
4. 한/영 문자열 전수 이관 완료 — 새 라벨은 예외 없이 `ui_str_id_t` + `refresh_lang_texts()`
   패턴을 따르는 걸로 확정(드롭다운 옵션 목록 문자열 포함, 영문 초/분/시간은 S/M/H로 축약).
5. 실기 크래시 2건 원인규명: (a) `ls`→`shot` FatFS `get_fattime()` 락 assert — 오늘 변경과
   무관한 기존 버그로 확인, 미해결로 남김. (b) 10초 자동촬영 간격이 ESP-NOW 트래픽과 겹쳐
   힙손상 — `clamp_capture_interval_sec()`(1800초 최소값)로 해결, UI에서 10초 옵션 제거.
6. CAM 절전: WiFi 모뎀슬립 적용+검증 완료. Automatic Light Sleep은 `esp_pm_lock`으로
   촬영/콘솔/ESP-NOW 구간을 보호했음에도 "명령 대기 중" 유휴 구간에서 USB 콘솔이 즉시
   먹통(보드 물리적으로 멈춰서 전원 재투입 필요) — 이번에도(2026-07-28에 이어 2번째) 전면
   원복.
7. **Ongoing 폴더 정리**: 백업/구버전/벤더 폴더를 `_Archive/`로 통합, 실체 없는 빌드
   찌꺼기 폴더 2개 삭제, `.gitignore`의 `.claude/*` 최상위-only 허점 발견+수정.
8. 사용자로부터 작업 태도 관련 지적 2건(묻지 않고 결정/행동하는 패턴, "대화에 집중해"
   구간에서는 읽기 전용 조사도 금지) — 둘 다 자동메모리에 등록해 앞으로 반영.

## 내일 할 일 (자세한 배경은 memory `project_cntl_rtc_and_unified_sleep_plan` 참고)

1. **RTC 시각 신뢰성 수정**
   - `Cntl/main/rtc_sync.c`의 부팅시 시드 로직에 체계적 버그 의심(컴파일 17:07 vs RTC
     02:17, 이후 실기 02:37 — 둘 다 새벽대라는 공통 패턴. `gmtime_r`/`mktime` 시간대 처리
     불일치 등 확인 필요).
   - System 설정 탭에 수동 시각설정 UI 추가.
   - 향후 인터넷 시간동기화 기능 — 구현 전에 UI부터 설계/논의.

2. **CAM 절전 재시도 — 우선순위 확정: Light Sleep 성공하면 Deep Sleep은 안 함**
   - 시도할 것: `CONFIG_USJ_NO_AUTO_LS_ON_CONNECTION` Kconfig 옵션(오늘 리서치로 찾음,
     USB 호스트가 실제로 연결되어 있는 동안만 자동 Light Sleep 진입을 막아줌 — 개발 중엔
     콘솔 보호, 실사용(USB 미연결)때는 정상 절전). 이걸로 콘솔 먹통 문제가 해결되는지 실기
     검증.
   - 실패 시에만: CAM도 SENS처럼 "응답성 주기마다 깨서(리셋 허용) 일하고 다시 자는" Deep
     Sleep 모델로 전환. 응답성이 짧으면(예: 10초) 원격 즉시깨움과 사실상 동등하다는 게
     사용자 판단 — ESP32 Deep Sleep은 구조적으로 WiFi/ESP-NOW 웨이크소스가 없음(공식
     확인됨, 별도 RF칩 없이는 원격기동 불가).
   - **나중에(지금 아님)**: Deep Sleep 타이머 웨이크소스가 버그로 안 걸려서 영원히 못 깨는
     상황을 방지하는 방법 리서치 — 아직 미착수.

3. 그 외 남은 항목(급하지 않음): CAM_CONFIG_SET의 `wb_mode`(화이트밸런스)는 수신만 하고
   센서에 미적용 — 계속 보류.

## Git

이번 세션 3개 커밋, **전부 `origin/main`에 push 완료** (아래 "Git push 결과" 절에서
최종 확인):

```
e13781a Ongoing: consolidate backup/old/vendor folders into _Archive
6c83f29 Cntl/CAM: Cntl-owned device config, Apply UX, bilingual audit, crash fixes
```
(이 문서 자체를 커밋하는 마지막 커밋 하나 더 있음 — `git log --oneline -5`로 최종 확인.)
